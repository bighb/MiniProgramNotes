<view class="title">weapp.qrcode.js 在 微信小程序 中，快速生成二维码</view>
<view class="card">
    <view>先在 wxml 文件中，创建绘制的 canvas，并定义好 width, height, canvasId 。</view>
    <view>
        直接引入 js 文件，使用 drawQrcode() 绘制二维码。!!!在 调用 drawQrcode() 方法之前，一定要确保可以获取到 canvas context 。
    </view>
</view>